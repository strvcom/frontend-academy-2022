export enum Routes {
  DASHBOARD = '/',
  LOGIN = '/login',
  CREATE_EVENT = '/events/create',
}

export enum ApiRoutes {
  LOGIN = '/api/login',
  LOGOUT = '/api/logout',
  REFRESH_TOKEN = '/api/refreshToken',
}
